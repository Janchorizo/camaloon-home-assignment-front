import React from 'react';
import {render} from 'react-dom';
// internal
import theme from 'common/theme.css'; // eslint-disable-line no-unused-vars
import {App} from 'components';

render(<App />, document.getElementById('react-root'));
